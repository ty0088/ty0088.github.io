<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="main-container">
        <h1><%= title %></h1>
        <nav>
            <a href="/">Home</a>
        </nav>
        <div class="content-container">
            <% if (typeof currentUser != 'undefined') { %>
                <p>Please change any of the information below and submit to update.</p>
            <% } else { %>
                <p>Please enter all of the information required below and then submit to sign up.</p>
            <% } %>
            <form action="" method="post">
                <div class="input-row">
                    <label for="username" class="input-label">Username: </label>
                    <input type="text" id="username" class="input-input" name="username" placeholder="username" maxlength="20" required value=<%- (typeof currUser == "undefined" ? "" : `"${currUser.username}"`) %>>
                    <span class="input-tip">(is required and must be no more than 10 characters long)</span>
                </div>
                <div class="input-row">
                    <label for="password" class="input-label">Password: </label>
                    <input type="password" id="password" class="input-input" name="password" minlength="8" maxlength="12" <%- (typeof currentUser != 'undefined' ? "" : "required") %>>
                    <% if (typeof currentUser != 'undefined') { %>
                        <span class="input-tip">(only required if you wish to change your password, else keep empty)</span>
                    <% } else { %>
                        <span class="input-tip">(is required and must be contain between 8 to 12 characters)</span>
                    <% } %>                     
                </div>
                <div class="input-row">
                    <label for="passwordConfirm" class="input-label">Confirm Password: </label>
                    <input type="password" id="passwordConfirm" class="input-input" name="passwordConfirm" minlength="8" <%- (typeof currentUser != 'undefined' ? "" : "required") %>>
                    <% if (typeof currentUser != 'undefined') { %>
                        <span class="input-tip">(only required if you wish to change your password, else keep empty)</span>
                    <% } else { %>
                        <span class="input-tip">(must match previously entered password)</span>
                    <% } %>   
                </div>
                <div class="input-row">
                    <label for="firstName" class="input-label">First Name: </label>
                    <input type="text" id="firstName" class="input-input" name="firstName" required value=<%- (typeof currUser == "undefined" ? "" : `"${currUser.firstName}"`) %>>
                    <span class="input-tip">(is required)</span>
                </div>
                <div class="input-row">
                    <label for="lastName" class="input-label">Last Name: </label>
                    <input type="text" id="lastName" class="input-input" name="lastName" required value=<%- (typeof currUser == "undefined" ? "" : `"${currUser.lastName}"`) %>>
                    <span class="input-tip">(is required)</span>
                </div>
                <div class="input-btns">
                    <button type="submit">Submit</button>
                    <button type="button" onclick=<%= goToUrl %>>Cancel</button>
                </div>
            </form>
            <% if (typeof errors != "undefined") { %>
                <hr>
                <h4>Errors!</h4>
                <ul>
                    <% for (const error of errors) { %>
                        <li><%= error.msg %></li>
                    <% } %>
                </ul>
            <% } %>
        </div>
    </div>
    <script src="/javascripts/goToUrl.js"></script>
</body>
</html>