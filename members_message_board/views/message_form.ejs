<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="main-container">
        <h1><%= title %></h1>
        <nav>
            <a href="/">Home</a>
            <a href=<%= currentUser.url %>>My Account</a>
            <a href="/log-out">Log Out</a>
        </nav>
        <div class="content-container">
            <form class="message-form" action="" method="post">
                <label for="messageText">Message: </label>
                <textarea name="messageText" id="messageText" rows="10" autofocus><%= typeof currMessage == "undefined" ? "" : currMessage.text %></textarea>
                <div class="input-btns">
                    <button type="submit">Post</button>
                    <button type="button" onclick=<%= goToUrl %>>Cancel</button>
                </div>
            </form>
            <% if (typeof errors != "undefined") { %>
                <hr>
                <h4>Errors!</h4>
                <ul>
                    <% for (const error of errors) { %>
                        <li><%= error.msg %></li>
                    <% } %>
                </ul>
            <% } %>
        </div>
    </div>
    <script src="/javascripts/goToUrl.js"></script>
</body>
</html>